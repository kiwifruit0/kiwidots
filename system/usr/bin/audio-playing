#!/bin/bash

# Check for active audio streams
if pw-dump | jq -e '.[] | select(.type == "PipeWire:Interface:Node" and .info.state == "running")' > /dev/null 2>&1; then
    echo "Audio is playing"
    exit 0
else
    echo "No audio playing"
    exit 1
fi
