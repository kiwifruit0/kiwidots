###################
### MY PROGRAMS ###
###################

$terminal = kitty
$fileManager = thunar
$browser = zen-browser

###################
### KEYBINDINGS ###
###################
$mainMod = SUPER # Sets "Windows" key as main modifier

# Application binds
bind = $mainMod, T, exec, $terminal
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, A, exec, pkill rofi || rofi -show drun -theme $HOME/.config/rofi/type-1/style-8.rasi
bind = $mainMod CONTROL, L, exec, hyprlock --no-fade-in
bind = $mainMod, F, exec, $browser
bind = $mainMod, D, exec, hyprshot -m region
bind = $mainMod SHIFT, D, exec, hyprshot -m window
bind = $mainMod CONTROL, D, exec, hyprshot -m output
bind = $mainMod SHIFT, A, exec, $HOME/.config/hypr/scripts/wiremix-toggle.sh
bind = $mainMod, R, exec, if pgrep waybar > /dev/null; then killall waybar; fi; waybar &
bind = $mainMod SHIFT, R, exec, killall waybar && $HOME/.config/hypr/scripts/flip_waybar.sh && waybar &

# window binds
bind = $mainMod, Q, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, P, pin
bind = $mainMod, V, togglesplit
bind = $mainMod SHIFT, F, fullscreen

# floating bind
bind = $mainMod, W, exec, hyprctl dispatch togglefloating active; \
  hyprctl -j activewindow | jq -e '.floating == true' && \
  hyprctl dispatch resizeactive exact 90% 70% && \
  hyprctl dispatch centerwindow

# Move focus with mainMod + vim directions
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Move windows around with mainMod + alt + vim arrows
bind = $mainMod ALT, H, swapwindow, l
bind = $mainMod ALT, J, swapwindow, d
bind = $mainMod ALT, K, swapwindow, u
bind = $mainMod ALT, L, swapwindow, r

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# resize windows with mainMod + shift + vim arrows
binde = $mainMod SHIFT, L, resizeactive, 30 0
binde = $mainMod SHIFT, H, resizeactive, -30 0
binde = $mainMod SHIFT, K, resizeactive, 0 -30
binde = $mainMod SHIFT, J, resizeactive, 0 30

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -n1 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -n2 set 5%-

# control music
bindl = ,XF86AudioNext, exec, playerctl next
bindl = ,XF86AudioPause, exec, playerctl play-pause
bindl = ,XF86AudioPlay, exec, playerctl play-pause
bindl = ,XF86AudioPrev, exec, playerctl previous
bindl = ,XF86AudioMicMute, exec, playerctl play-pause

bindl = $mainMod SHIFT, right, exec, playerctl next
bindl = $mainMod SHIFT, left, exec, playerctl previous
bindl = $mainMod SHIFT, space, exec, playerctl play-pause

# wallpaper changer
# bind = $mainMod, N, exec, killall hyprpaper && $HOME/.config/hypr/scripts/random_wallpaper.sh && hyprpaper


